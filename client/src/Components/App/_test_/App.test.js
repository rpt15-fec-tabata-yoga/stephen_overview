import React from 'react';
import { shallow, mount, render } from 'enzyme';
import App from '../App.jsx';

test('API call with gameId should return information for that game', (done) => {
  request('http://localhost:3000/')
})